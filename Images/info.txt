add wireframe for admin and super admin

Fixes to existing wireframes:
- signup page needs an age/date of birth field, needed for age restricted groups, currently nowhere to enter this
- chat page needs a room member list / who's currently in the room, plus join/leave notifications
- chat page needs a visual badge/indicator on messages from the group admin
- settings page: decide if the Profile panel (email/username/password rows) should link to the change pages, or is just a display, currently duplicates the Settings panel buttons

New wireframes needed:
- group admin dashboard
- super admin dashboard
- browse/find groups page (shown from "Find Groups" button), lists all groups with age limit, request to join button
- request new room - TBD, leaning towards a small popup/modal from within the group rather than a full page, so it still feels like "just asking" but the request can be tracked (need name + reason on reject)
- request new group - can't be done as a chat message since users can't message the super admin directly, needs at least a small form/modal to capture title, description, age limit, colour theme
- pending/rejected requests view - tracks both group and room requests the user has submitted, shows type, target name, status, date submitted, and rejection reason if rejected (approved ones just become the real group/room so don't need to stay listed)
- report/ban a user flow - for a user reporting someone, and the group admin actioning the report
- group admin member management - promote/demote admin, view and ban members
- super admin audit log page, filterable by type and date